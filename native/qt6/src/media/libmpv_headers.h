#ifndef LIBMPV_HEADERS_H
#define LIBMPV_HEADERS_H

// Vendored official libmpv API headers to avoid relying on system -devel
// packages on developer machines. Runtime still loads the system libmpv.so.

#include "mpv/client.h"
#include "mpv/render.h"
#include "mpv/render_gl.h"

#endif
